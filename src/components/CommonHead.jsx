import React from "react";

const CommonHead = ({ subhead }) => {
  return (
    <div className="text-white">
      <h1 className="text-5xl font-semibold">{subhead}</h1>
    </div>
  );
};

export default CommonHead;
